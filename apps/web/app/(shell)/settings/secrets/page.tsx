import { Placeholder } from "../../placeholder";

/** `/settings/secrets` — the Organization's vault, when its slice lands. */
export default function SecretsPage() {
  return (
    <Placeholder>
      The Secrets vault arrives with its own slice: named, encrypted values a Workflow&apos;s secret
      Variables bind to by name.
    </Placeholder>
  );
}
