import { Placeholder } from "../../placeholder";

/** `/settings/extension` — the pill's full surface, when its slice lands. */
export default function BrowserExtensionPage() {
  return (
    <Placeholder>
      The browser extension&apos;s surface arrives with its own slice: the download, the install
      steps, how to point it at this instance, and the version it is on.
    </Placeholder>
  );
}
