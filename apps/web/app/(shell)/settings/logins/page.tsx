import { Placeholder } from "../../placeholder";

/** `/settings/logins` — the saved browser state, when its slice lands. */
export default function SavedLoginsPage() {
  return (
    <Placeholder>
      Saved logins arrive with their own slice: the signed-in browser state a Run receives for the
      sites it touches.
    </Placeholder>
  );
}
