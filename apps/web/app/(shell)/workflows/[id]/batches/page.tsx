import { Placeholder } from "../../../placeholder";

/**
 * Workflow ▸ Batches — the only home for a list of Batches, now that a global
 * Batches index is refused.
 */
export default function BatchesTab() {
  return (
    <Placeholder>
      This Workflow&rsquo;s Batches arrive with the Batch spec. There is no global Batches index —
      this tab is where they live.
    </Placeholder>
  );
}
