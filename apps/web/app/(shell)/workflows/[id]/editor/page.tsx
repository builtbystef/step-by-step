import { Placeholder } from "../../../placeholder";

/**
 * Workflow ▸ Editor — the tab a Workflow opens on, and the one a new Workflow
 * lands on empty.
 *
 * The editor itself is `d8ux2s`'s: the Step list, recording, and publishing.
 */
export default function EditorTab() {
  return (
    <Placeholder>
      The Step editor arrives with the recording spec — the Step list, recording into this Draft,
      and publishing it as a Version.
    </Placeholder>
  );
}
