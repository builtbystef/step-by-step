import { Placeholder } from "../../../placeholder";

/**
 * Workflow ▸ Schedules — the global Schedules list filtered to this Workflow,
 * and the same component rendering it (`<SchedulesList workflowId>`).
 */
export default function SchedulesTab() {
  return (
    <Placeholder>
      This Workflow&rsquo;s Schedules arrive with the shared Schedules list, which is the global
      list with one prop set.
    </Placeholder>
  );
}
